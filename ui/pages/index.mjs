const HomePage = () => (
  <main className={`flex min-h-screen flex-col items-center justify-between p-24`}>
    <button className="btn btn-primary">morio</button>
  </main>
)

export default HomePage
