# Variable Selector

Morio's **Variable Selector** allows you to select a **variable** from
the [variables defined in settings](/settings/tokens/vars).

Variables ares typically used to keep settings _DRY_ or facilitate making changes
to settings that are not exposed via the UI.

<Note compact noP>
  Before you can select a secret, you must first create it.
</Note>
