Identity and Access Management (IAM) is all about **who** can do **what**. 

The `who` part is identity, and the `what` part is access management.
Together, we refer to them as IAM.

IAM strives to ensure that users have access to the correct resources.

:::tip
[Wikipedia is a good source for more information on
IAM](https://en.wikipedia.org/wiki/Identity_management)
:::

