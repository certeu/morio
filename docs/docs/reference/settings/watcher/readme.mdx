---
title: 'Morio Settings: watcher'
sidebar_label: watcher
---

The `watcher` settings hold settings for the _watcher service_, which is Elastic Heartbeat under the hood.

The entire `watcher` key is <Label>optional</Label>. Without it, no watcher service will be started.

<Fixme>Document these settings</Fixme>
