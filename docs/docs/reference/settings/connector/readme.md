---
title: 'Morio Settings: connector'
sidebar_label: connector
---

The `connector` settings hold settings for the _connector service_, which is Elastic Logstash under the hood.

The entire `connector` key is <Label>optional</Label>. Without it, no connector service will be started.

<Fixme>Document these settings</Fixme>
